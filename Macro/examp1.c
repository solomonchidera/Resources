#include "main.h"

/* Defining a macro called MAX_VALUE*/
#define MAX_VALUE 100

int main(void)
{
	int num;
	num = 75;
	
	if (num < MAX_VALUE)
	{
		printf("Num is less that MAX_VALUE\n");
	}
	return (0);

}
